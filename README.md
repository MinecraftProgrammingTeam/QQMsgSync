# QQMsgSync
MC服务器 <==> QQ群消息 双向同步

---

## 使用
1. 将插件jar放置在plugins目录中，启动服务器；
2. 修改`config.yml`中的机器人相关配置；
3. 添加机器人到要同步的群聊中，并发送`/获取ID`（注意大小写）；
4. 将获取到的id写入到`config.yml`中，重启服务器
5. 在要同步的群聊中发送`/绑定群聊`

## LICENSE
Apache License 2.0 · 不得商用